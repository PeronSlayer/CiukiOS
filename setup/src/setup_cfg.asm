; CiukiOS Setup - Configuration Writer
; TODO:
;   - write_ciukios_cfg: create CIUKIOS.CFG on installed volume
;     fields: VIDEO=mode, LOCALE=en, TIMEZONE=UTC, GEM=1/0
;   - write_autoexec: create minimal AUTOEXEC.BAT equivalent
;   - write_config: create CONFIG.SYS equivalent (FILES=, BUFFERS=, etc.)

bits 16
