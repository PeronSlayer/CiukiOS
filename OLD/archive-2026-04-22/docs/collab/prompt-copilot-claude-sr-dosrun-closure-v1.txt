You are working on CiukiOS. Execute ONLY this task pack:
`docs/collab/copilot-task-claude-sr-dosrun-closure-v1.md`

Hard requirements:
1) Create and use dedicated branch only:
   `feature/copilot-claude-sr-dosrun-closure-v1` from `origin/main`.
2) Do not modify `main`.
3) Implement all scope items D1..D5.
4) Run all validation commands listed in the task pack.
5) If a test fails, fix and re-run until green.
6) Produce final handoff file exactly as requested in the task pack.
7) Keep token usage low: concise reasoning, concise commits, no repeated verbose status.
8) Do not remove existing deterministic markers required by current tests.

Output format at the end (strict):
- Branch name
- Commits created (hash + title)
- Tests run + PASS/FAIL
- Handoff file path
- Residual risks/limits (max 5 bullets)
