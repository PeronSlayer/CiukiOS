# Changelog

All notable project-level changes are tracked here.
This changelog is intentionally concise. Every completed task should update `Unreleased` unless the task cuts a release section.

## Unreleased (2026-05-11)

1. Restored the current DOOM/WOLF3D runtime branch around a compact Stage1 MZ/DOS memory layout: primary EXE loading now has a separate MZ load limit, low DOS scratch buffers are isolated from the loader window, MZ tail memory is cleared before handoff, and runtime cache state is reset before external MZ execution.
2. Fixed the DOOM launch regression that stalled at `V_Init: allocate screens`; the dedicated DOOM taxonomy lane now reaches `visual_gameplay=PASS` on the full profile.
3. Fixed WOLF3D black-screen startup in local full-image builds by patching only the injected copy of `WOLF3D.EXE` to bypass the stuck page-flip wait. The source payload under `third_party/WOLF3D` remains untouched, and the final WOLF3D capture is non-black (`720x400`, `colors=6`, `nonblack=24336`).
4. Added SB16 probe/playback evidence through `SB16INIT.COM`, `DRVLOAD.COM /AUDIO`, and QEMU SB16 device wiring. QEMU audio now defaults to on across full, full-CD, taxonomy, and DRVLOAD smoke runners while still allowing `QEMU_AUDIO_MODE=off` for explicit silent runs; the validated path detects the DSP at `0x220`, configures IRQ/DMA mixer state, and completes a real DMA1/IRQ7 SB playback probe. DOOM now launches as `DOOM.EXE`, removing the old `-nosound` and `-nosfx` workarounds while the generated music-only config keeps the unstable SB SFX/DMA path disabled via `snd_channels=0` and `snd_sfxdevice=0`, with the DOOM taxonomy pinned to the ALSA backend to avoid a local PipeWire/OPL QEMU crash, while SB DMA/IRQ sound effects remain follow-up work.
5. Kept the full-CD build inside the Stage1 size budget by making the legacy Stage2 autorun/hardware-validation CD path opt-in instead of forced by default; the active full-CD gate remains the Live/install D: prompt smoke.
6. Restored the README as a complete project entry point, condensed this changelog to release-level facts, and removed temporary local run artifacts while keeping generated build outputs out of the tracked worktree.
7. Improved shell external-command dispatch with GPLv2 FreeCOM-style first-word/rest splitting and `DoExec`-style command-tail packing, so direct `PROGRAM ARG` launches pass the argument tail like the existing `run PROGRAM ARG` path and quoted external command names are parsed without changing prompt text or shell UX; the shared full/full-cd DOS environment now exposes `PATH=C:\APPS;C:\SYSTEM\DRIVERS` for external programs.
8. Relicensed CiukiOS project code from GPLv3 to GPLv2 so GPLv2 FreeDOS/FreeCOM code can be integrated directly, removed the prebuilt FreeCOM binary packaging shortcut, and documented the source-port policy.
9. Added a reversible DOOM audio-profile packaging switch for full/full-cd (`CIUKIOS_DOOM_AUDIO_PROFILE=music-only|sb16`) and a narrow taxonomy pre-launch probe that prints `DEFAULT.CFG` before `run DOOM.EXE`, so runtime investigations can distinguish the intentional music-only bypass from an SB16-enabled launch without changing shell UX or the single-command DOOM run path.
10. Removed the FAT16 shell footer/status strip entirely from Stage1 by turning the footer update/poll path into no-ops and by dropping the remaining decorative horizontal separator lines from the shell chrome, keeping the shell prompt/output UX simpler while recovering Stage1 code budget on both full and full-CD builds.
11. Added `AUDIOTST.COM`, a tiny SB16-focused audio utility that probes the DSP at legacy bases and plays three short DMA-backed test tones with serial/log markers, and packaged it into `\SYSTEM\DRIVERS` for full/full-CD images together with a short `AUDIO.COM` alias for easier invocation from the shell.
12. Fixed bare shell execution of driver helpers from the default `C:\APPS>` prompt by extending Stage1 external-command resolution to probe `\SYSTEM\DRIVERS` in addition to CWD and `\APPS`, so short commands like `AUDIO`, `AUDIOTST`, `SB16INIT`, and `DRVLOAD` no longer require `run ...` or a manual `cd \SYSTEM\DRIVERS`.
13. Expanded `AUDIOKEY.COM` into a more usable interactive SB16 keyboard tester: the arrow keys still hit preset tones, `C D E F G A B` play scale notes, `+` and `-` fine-tune the current pitch in single steps, `SPACE` replays the current note, and the on-screen legend was rewritten to show the controls more clearly without changing the surrounding shell UX.
14. Hardened the DOS child audio environment for legacy SB16-aware apps by exporting a fuller `BLASTER=A220 I7 D1 H5 T6` string from Stage1 instead of the older truncated `A220 I7 D1`, and documented the current DOOM finding that stable SB16 engagement appears to need explicit prelaunch helper priming (`DRVLOAD /AUDIO`) on the real audio-enabled taxonomy lane rather than hidden hardware mutation inside the Stage1 MZ exec path.
15. Added a new `DOOMSB.COM` launcher into `\APPS\DOOM` that copies the bundled SB16 config into both `\APPS\DOOM\DEFAULT.CFG` and `\DOOMDATA\DEFAULT.CFG`, runs `DRVLOAD /AUDIO`, and then launches `DOOM.EXE`, so the current best-known SB16 priming sequence can be invoked as one command without touching the Stage1 MZ exec hot path.
16. Added a short `AKEY.COM` alias for `AUDIOKEY.COM` in `\SYSTEM\DRIVERS`, matching the existing `AUDIO.COM` convenience alias so the interactive keyboard audio tester is faster to launch from the shell.
17. Added quiet-mode support to `SB16INIT.COM` and `DOOMSB.COM`, then ruled out the silent launcher approach for default DOOM startup because the nested helper path and every confirmed SB/DMX SFX path currently stall before visual gameplay; the default DOOM profile remains the stable music-only visual lane while `CIUKIOS_DOOM_AUDIO_PROFILE=sb16-sfx` stays available for focused SFX investigation.
18. Hardened the DOS app visual taxonomy so doubled serial DOOM markers such as `SS__IInniitt` / `HHUU__IInniitt` count as late video evidence, and so a fresh high-diversity screenshot can classify `visual_gameplay` even if the local QEMU audio backend crashes during post-capture shutdown; this keeps runtime stability failures visible without hiding concrete visual evidence.
19. Switched the packaged default DOOM audio profile from silent/music-only SFX bypass to a stable PC speaker SFX fallback (`snd_sfxdevice=1`, `snd_musicdevice=0`, `snd_channels=8`) and wired QEMU full/full-CD/taxonomy runners with `pcspk-audiodev=snd0`, so DOOM has an in-game audio path now while the broken SB/DMX protected-mode SFX branch remains opt-in via `CIUKIOS_DOOM_AUDIO_PROFILE=sb16-sfx`.
20. Changed local QEMU audio backend auto-selection to prefer ALSA before PipeWire for full/full-CD/taxonomy/DRVLOAD lanes, improving the chance that PC speaker DOOM SFX are actually audible instead of only configured.
21. Added narrow serial-only vector tracing for DOS `INT 21h AH=25h/35h` on timer/IRQ vectors `08h`, `0Dh`, `0Fh`, and `1Ch`, so DOOM/DMX protected-mode audio work can distinguish real-mode vector hooks from DOS/4GW internal protected-mode interrupt handling without changing shell UX.
22. Added and fixed a real DOS/4GW protected-mode SB IRQ/DMA probe: `PMIRQSB.COM` launches `DOS4GW.EXE PMIRQSB.LE`, installs DPMI protected-mode interrupt vectors with `INT 31h AX=0204/0205`, checks virtual IRQ enable, verifies timer IRQ reflection, waits on that protected-mode timer long enough for SB16 DMA completion, validates DMA buffers do not cross a 64K boundary, and now covers single-cycle ACK-before-EOI, single-cycle EOI-before-ACK, and auto-init DMA variants without faking any INT31 support in Stage1.
23. Captured a focused DOOM DMX/SB16 reverse handoff with the exact local `DOOM.EXE` identity, embedded DOS/4G LE payload offset, public PCDoom/Doom Vanille/Apogee runtime map, observed DOOM startup markers, and the narrowed next step: a DMX-like protected-mode timer/task/FX-DMA probe in `PMIRQSB` before touching Stage1 or guessing at DOOM config again.
24. Extended `PMIRQSB` with an opt-in `TASK` mode that installs DOS/4GW protected-mode timer/SB IRQ vectors and dispatches repeated timer-paced single-cycle DMA playbacks with `[PMIRQSB] TASK HIT`, `FXDMA START`, `FXDMA IRQ`, and `TASK PASS` markers, targeting the DMX `TSM_NewService`/`WAV_PlayMode` style failure surface without changing Stage1 or user-facing shell UX.

Validation evidence:
- `make build-full` PASS.
- `make qemu-test-full` PASS.
- `make verify-full-drivers-payload` PASS with 64 driver files including `SB16INIT.COM`.
- `make qemu-test-full-doom-taxonomy` PASS with `visual_gameplay=PASS`, QEMU audio on, and the DOOM visual gate launched as `run DOOM.EXE` using music-enabled audio config with SFX channels disabled on the ALSA backend.
- WOLF3D full-profile taxonomy PASS at `runtime_stable`, with `build/full/wolf-final-visible.ppm` stats `720x400 colors=6 nonblack=24336`.
- `DRVLOAD_ARGS=/AUDIO QEMU_TIMEOUT_SEC=260 bash scripts/qemu_test_full_drvload_smoke.sh` PASS with QEMU audio on (`backend=pipewire`, SB16 `0x220`, IRQ 7), including `DSP OK at 0x0220`, `DMA DONE`, `TONE DONE`, and `OK AUDIO` markers.
- `make build-full-cd` PASS.
- `CIUKIOS_DOOM_AUDIO_PROFILE=sb16 make build-full` PASS; the injected `build/full/obj/doom-default.cfg` now carries `snd_channels=8`, `snd_sfxdevice=3`, `snd_sbport=544`, `snd_sbirq=7`, `snd_sbdma=1`, and `snd_mport=816`.
- `DO_BUILD=0 CIUKIOS_DOOM_AUDIO_PROFILE=sb16 make qemu-test-full-doom-taxonomy` FAIL at `visual_gameplay` but PASS at `runtime_stable`; serial startup still reaches `I_StartupSound`, `calling DMX_Init`, and `S_Init: Setting up sound`, while also reporting `Dude.. The Adlib isn't responding.` before the screen regresses to a low-diversity frame.
- `CIUKIOS_DOOM_AUDIO_PROFILE=sb16 make build-full`, followed by injecting a no-music DOOM config (`snd_musicdevice=0`, `snd_mport=-1`, `snd_sfxdevice=3`) into `::APPS/DOOM/DEFAULT.CFG` and `::DOOMDATA/DEFAULT.CFG`, then `DO_BUILD=0 make qemu-test-full-doom-taxonomy`: FAIL at `visual_gameplay` but PASS at `runtime_stable`; the serial log still reaches `I_StartupSound`, `calling DMX_Init`, and `S_Init: Setting up sound`, but the AdLib complaint disappears, narrowing the remaining fault away from the music fallback and toward the SB/DMX sound-effects path or its interaction with gameplay rendering.
- `make build-full` PASS after the shell-chrome cleanup and audio-tool integration; `verify-full-drivers-payload` now passes with 66 files including `SB16INIT.COM`, `AUDIOTST.COM`, and the `AUDIO.COM` alias.
- `DO_BUILD=0 DOS_TAXONOMY_USE_CASE=generic DOS_TAXONOMY_PROFILE=dos_generic DOS_TAXONOMY_MIN_STAGE=transfer_marker DOS_TAXONOMY_APP_DIR_IN_IMAGE=::SYSTEM/DRIVERS DOS_TAXONOMY_APP_BINARY_NAME=AUDIOTST.COM DOS_TAXONOMY_CWD='\SYSTEM\DRIVERS' DOS_TAXONOMY_RUN_COMMAND='run AUDIOTST.COM' DOS_TAXONOMY_APP_RUNTIME_MARKERS='\[AUDIOTST\][[:space:]]+DONE' DOS_TAXONOMY_RUN_DRVLOAD=0 QEMU_AUDIO_MODE=on QEMU_AUDIO_BACKEND=alsa QEMU_TIMEOUT_SEC=240 bash scripts/qemu_test_full_dos_taxonomy.sh` PASS at `transfer_marker`; fresh serial output shows `[AUDIOTST] DSP OK at 0x0220`, `TONE 1`, `DMA DONE`, `TONE 2`, `DMA DONE`, `TONE 3`, `DMA DONE`, and `DONE`. The same utility intentionally fails `runtime_stable` because it exits back to the shell after completing.
- `DO_BUILD=0 DOS_TAXONOMY_USE_CASE=generic DOS_TAXONOMY_PROFILE=dos_generic DOS_TAXONOMY_MIN_STAGE=transfer_marker DOS_TAXONOMY_PROMPT_TIMEOUT_SEC=180 DOS_TAXONOMY_APP_DIR_IN_IMAGE=::SYSTEM/DRIVERS DOS_TAXONOMY_APP_BINARY_NAME=AUDIO.COM DOS_TAXONOMY_CWD='\SYSTEM\DRIVERS' DOS_TAXONOMY_RUN_COMMAND='run AUDIO.COM' DOS_TAXONOMY_APP_RUNTIME_MARKERS='\[AUDIOTST\][[:space:]]+DONE' DOS_TAXONOMY_RUN_DRVLOAD=0 QEMU_AUDIO_MODE=on QEMU_AUDIO_BACKEND=alsa QEMU_TIMEOUT_SEC=240 bash scripts/qemu_test_full_dos_taxonomy.sh` PASS at `transfer_marker`; the short `AUDIO.COM` alias reaches the same three-tone `[AUDIOTST]` sequence and then returns to the shell by design.
- `make build-full` PASS after extending Stage1 bare-command resolution to probe `\SYSTEM\DRIVERS`.
- `DO_BUILD=0 IMG=build/full/ciukios-full-audio-bare2.img LOG_FILE=build/full/qemu-audio-bare2.log QEMU_STDERR=build/full/qemu-audio-bare2.stderr QEMU_CMD_LOG=build/full/qemu-audio-bare2.cmd DOS_TAXONOMY_USE_CASE=generic DOS_TAXONOMY_PROFILE=dos_generic DOS_TAXONOMY_MIN_STAGE=transfer_marker DOS_TAXONOMY_PROMPT_TIMEOUT_SEC=180 DOS_TAXONOMY_APP_DIR_IN_IMAGE=::SYSTEM/DRIVERS DOS_TAXONOMY_APP_BINARY_NAME=AUDIO.COM DOS_TAXONOMY_CWD='' DOS_TAXONOMY_RUN_COMMAND='AUDIO' DOS_TAXONOMY_APP_RUNTIME_MARKERS='\[AUDIOTST\][[:space:]]+DONE' DOS_TAXONOMY_RUN_DRVLOAD=0 QEMU_AUDIO_MODE=on QEMU_AUDIO_BACKEND=alsa QEMU_TIMEOUT_SEC=240 bash scripts/qemu_test_full_dos_taxonomy.sh` PASS at `transfer_marker`; the fresh serial log shows bare `AUDIO` typed directly at the default `C:\APPS>` prompt and then emitting the full `[AUDIOTST]` tone sequence through `DONE` before returning to the prompt.
- `make qemu-test-full-dos-compat-smoke` PASS, including DOS21, GFXSTAR, and DOSNavigator startup coverage.
- `make qemu-test-full-cd` PASS with Live CD prompt `D:`.
- `CIUKIOS_DOOM_AUDIO_PROFILE=sb16 make build-full-cd` PASS.
- `make build-full-cd` PASS after the Stage1/footer and audio-tool changes, and `make qemu-test-full-cd` PASS with Live CD prompt `D:`.
- `make qemu-test-full` PASS after removing the shell separator lines from `draw_shell_chrome`.
- `make build-full` PASS after extending `AUDIOKEY.COM` with scale-letter note keys, `+/-` fine-tune controls, `SPACE` replay, and the rewritten keyboard legend; `verify-full-drivers-payload` continues to pass with 67 driver files including `AUDIOKEY.COM`.
- `make build-full-cd` PASS after packaging the updated `AUDIOKEY.COM` into the full-CD image set.
- `DO_BUILD=0 DOS_TAXONOMY_USE_CASE=generic DOS_TAXONOMY_PROFILE=dos_generic DOS_TAXONOMY_MIN_STAGE=runtime_stable DOS_TAXONOMY_APP_DIR_IN_IMAGE=::SYSTEM/DRIVERS DOS_TAXONOMY_APP_BINARY_NAME=AUDIOKEY.COM DOS_TAXONOMY_RUN_COMMAND='AUDIOKEY' DOS_TAXONOMY_POST_LAUNCH_KEY='equal' QEMU_AUDIO_MODE=on QEMU_AUDIO_BACKEND=alsa QEMU_TIMEOUT_SEC=240 bash scripts/qemu_test_full_dos_taxonomy.sh` ended with `runtime_stable=PASS` but overall `FAIL` because the harness did not freshly detect the shell prompt before launch in that run; this leaves the new interactive `+` key path unverified in automation even though the packaged binary is present and the runtime window stayed controllable.
- `make build-full` PASS after exporting `BLASTER=A220 I7 D1 H5 T6` from Stage1 for DOS child processes.
- `make build-full-cd` PASS after carrying the same Stage1 `BLASTER` environment hardening into the full-CD image set.
- On the real audio-enabled generic DOS taxonomy lane with a forced no-music SB16 DOOM config (`snd_channels=8`, `snd_musicdevice=0`, `snd_sfxdevice=3`, `snd_sbport=544`, `snd_sbirq=7`, `snd_sbdma=1`, `snd_mport=-1`), direct `run DOOM.EXE` still reached `runtime_stable=PASS` and `visual_gameplay=PASS` but continued to report `SB isn't responding at p=0x220, i=7, d=1`, indicating a likely stable fallback instead of confirmed SB16 SFX engagement.
- On the same audio-enabled generic DOS taxonomy lane, adding a single prelaunch `DRVLOAD /AUDIO` command before `run DOOM.EXE` preserved `runtime_stable=PASS` and `visual_gameplay=PASS`, while the fresh log showed `[SB16INIT] DSP OK at 0x0220`, `[SB16INIT] DMA DONE`, `[SB16INIT] TONE DONE`, and then DOOM startup without the earlier `SB isn't responding` line before `calling DMX_Init` / `S_Init`, making explicit prelaunch SB16 priming the strongest current explanation for why DOOM otherwise falls back or destabilizes.
- `make build-full` PASS after adding `DOOMSB.COM` packaging to `\APPS\DOOM`.
- `make build-full-cd` PASS after carrying the same `DOOMSB.COM` launcher into the full-CD DOOM payload.
- `DOOMSB.COM` automation is not yet a clean PASS: the launcher assembles and is injected, but the attempted fresh taxonomy run around `run DOOMSB.COM` stalled at boot/prompt-detection level before reliable runtime markers were produced, so the one-command launcher path remains packaged-but-not-fully-validated.
- `make build-full` PASS after adding the `AKEY.COM` alias for `AUDIOKEY.COM`.
- `make build-full-cd` PASS after carrying the same `AKEY.COM` alias into the full-CD image set.
- `make build-full` PASS after adding quiet-mode support to `SB16INIT.COM` and `DOOMSB.COM`, adding the opt-in `sb16-sfx` DOOM config profile, and keeping default DOOM startup on the stable music-only profile.
- `DO_BUILD=0 LOG_FILE=build/full/qemu-doom-sb16-clean.log QEMU_STDERR=build/full/qemu-doom-sb16-clean.stderr QEMU_CMD_LOG=build/full/qemu-doom-sb16-clean.commands.log DOS_TAXONOMY_USE_CASE=doom DOS_TAXONOMY_PROFILE=dosapp DOS_TAXONOMY_PROMPT_TIMEOUT_SEC=180 DOS_TAXONOMY_MARKER_TIMEOUT_SEC=60 DOS_TAXONOMY_SCREENSHOT_DELAY_SEC=25 DOS_TAXONOMY_OBSERVE_SEC=65 DOS_TAXONOMY_MIN_STAGE=visual_gameplay DOS_TAXONOMY_RUN_DRVLOAD=0 DOS_TAXONOMY_PRE_LAUNCH_COMMAND='__none__' DOS_TAXONOMY_RUN_COMMAND='run DOOM.EXE' DOS_TAXONOMY_SCREENSHOT='build/full/qemu-doom-sb16-clean.ppm' QEMU_AUDIO_MODE=on QEMU_AUDIO_BACKEND=alsa QEMU_TIMEOUT_SEC=390 bash scripts/qemu_test_full_dos_taxonomy.sh` FAIL at `visual_gameplay`; fresh logs reach `SB_Detect returned p=0x220,i=7,d=1`, `DMX_Init() returned 8`, `S_Init`, and `HU_Init`, then remain on the low-diversity startup screen, confirming the remaining blocker is the real SB/DMX SFX runtime path rather than the taxonomy classifier.
- `QEMU_SB_IRQ=5` with matching `snd_sbirq=5`, `BLASTER` set to SBPro-compatible `T4`, and alternate `snd_sfxdevice` values did not produce a stable visual+SFX lane; IRQ5 and SBPro typing still stall after DMX init, while the PC-speaker/alternate device probes either exit back to the shell or crash QEMU under the current audio backend.
- `make qemu-test-full-doom-taxonomy` PASS again on the default `music-only` profile, reaching `visual_gameplay=PASS` with screenshot diversity `640x400`, `nonblank_samples=21064`, and `unique_sampled_colors=107`; this confirms the SB/DMX SFX investigation no longer regresses the stable DOOM visual lane.
- `make qemu-test-full-cd` PASS with Live CD prompt `D:` after the DOOM SFX investigation changes.
- A focused SB detection/no-playback config (`snd_sfxdevice=3`, `snd_channels=0`, `snd_musicdevice=0`) still reached `SB_Detect returned p=0x220,i=7,d=1`, `DMX_Init() returned 8`, `S_Init`, and `HU_Init`, then remained on the low-diversity startup screen; this narrows the DOOM blocker to entering the SB/DMX protected-mode sound path itself, not just active voice mixing.
- A temporary Stage1 IRQ5/IRQ7 PIC unmask experiment did not fix the SB/DMX visual stall and was not kept, avoiding an unproven real-hardware interrupt side effect without a protected-mode IRQ bridge.
- `bash -n scripts/qemu_test_full_dos_taxonomy.sh` PASS after the visual taxonomy matcher/gating hardening.
- `make qemu-test-full-doom-taxonomy` PASS on the default `music-only` profile after the taxonomy hardening, reaching `visual_gameplay=PASS` with screenshot diversity `640x400`, `nonblank_samples=21137`, and `unique_sampled_colors=106`.
- `CIUKIOS_DOOM_AUDIO_PROFILE=sb16-sfx make build-full` PASS, followed by the single-command SB lane `run DOOM.EXE` with `DOS_TAXONOMY_PRE_LAUNCH_COMMAND='__none__'`: taxonomy now reports the real failure as `runtime_stable=PASS` and `visual_gameplay=FAIL` with low-diversity screenshot stats `720x400`, `nonblank_samples=1914`, `unique_sampled_colors=3`, after DOOM logs `I_StartupSound`, `calling DMX_Init`, `S_Init`, and `HU_Init`.
- `make qemu-test-full-cd` PASS with Live CD prompt `D:` after the taxonomy hardening.
- `make build-full` PASS with default `pcspeaker-sfx`, generating DOOM config `snd_channels=8`, `snd_musicdevice=0`, `snd_sfxdevice=1`, and `snd_mport=-1`.
- `make qemu-test-full-doom-taxonomy` PASS with default `pcspeaker-sfx` and QEMU `pcspk=on`, reaching `visual_gameplay=PASS` with screenshot diversity `640x400`, `nonblank_samples=21139`, and `unique_sampled_colors=106`; the local QEMU process can still exit with SIGILL/SIGSEGV during audio backend cleanup, but the taxonomy now preserves the fresh visual evidence.
- `make qemu-test-full` PASS with QEMU audio detail `backend=pipewire pcspk=on sb16=iobase=0x220 irq=7 dma=1 hdma=5`.
- `make qemu-test-full-cd` PASS with Live CD prompt `D:` and QEMU audio detail `backend=pipewire pcspk=on sb16=iobase=0x220 irq=7 dma=1 hdma=5`.
- An explicit QEMU `adlib` device plus DOOM AdLib music was tested and rejected because it regressed DOOM back to the low-diversity startup screen; the shipped audible fallback remains PC speaker SFX with music disabled.
- `make build-full` PASS after preferring ALSA for QEMU audio; default DOOM remains `pcspeaker-sfx`.
- `make qemu-test-full-doom-taxonomy` PASS after the ALSA preference, reaching `visual_gameplay=PASS` with screenshot diversity `640x400`, `nonblank_samples=21135`, and `unique_sampled_colors=110`; local QEMU still hit SIGSEGV during audio cleanup, but fresh visual evidence was preserved.
- `make qemu-test-full` PASS with QEMU audio detail `backend=alsa pcspk=on sb16=iobase=0x220 irq=7 dma=1 hdma=5`.
- `make build-full` PASS after adding the serial-only vector tracing.
- `CIUKIOS_DOOM_AUDIO_PROFILE=sb16-sfx make build-full` PASS, followed by a focused SB16 DOOM run with `DOS_TAXONOMY_PRE_LAUNCH_COMMAND='__none__'`: `runtime_stable=PASS`, `visual_gameplay=FAIL`, and no `[V25]`/`[V35]` vector markers for timer/IRQ vectors during DOOM startup, indicating DMX is not installing those handlers through real-mode DOS `INT 21h`.
- The same SB16 lane with explicit `SB16INIT /Q` prelaunch emitted `[V25] 0F 2000:02C5` and `[V25] 0F F000:D957` only around the helper's IRQ7 hook/restore, then DOOM still failed visual gameplay; this confirms real-mode IRQ7 priming works but does not satisfy DOOM's protected-mode SB/DMX path.
- `make build-full` PASS after packaging `PMIRQSB.COM`, `PMIRQSB.LE`, and `DOS4GW.EXE`; `verify-full-drivers-payload` passes with 71 driver files.
- `DO_BUILD=0 LOG_FILE=build/full/qemu-pmirqsb-diag2.log QEMU_STDERR=build/full/qemu-pmirqsb-diag2.stderr QEMU_CMD_LOG=build/full/qemu-pmirqsb-diag2.commands.log DOS_TAXONOMY_USE_CASE=generic DOS_TAXONOMY_PROFILE=dos_generic DOS_TAXONOMY_MIN_STAGE=transfer_marker DOS_TAXONOMY_APP_DIR_IN_IMAGE=::SYSTEM/DRIVERS DOS_TAXONOMY_APP_BINARY_NAME=PMIRQSB.COM DOS_TAXONOMY_CWD='\SYSTEM\DRIVERS' DOS_TAXONOMY_RUN_COMMAND='run PMIRQSB.COM' DOS_TAXONOMY_APP_RUNTIME_MARKERS='\[PMIRQSB\][[:space:]]+PASS' DOS_TAXONOMY_RUN_DRVLOAD=0 QEMU_AUDIO_MODE=on QEMU_AUDIO_BACKEND=alsa QEMU_TIMEOUT_SEC=240 bash scripts/qemu_test_full_dos_taxonomy.sh` PASS at `transfer_marker`; fresh markers show `[PMIRQSB] DSP OK`, `[PMIRQSB] PMVEC OK`, `[PMIRQSB] VIRQ ON`, `[PMIRQSB] DMA START`, `[PMIRQSB] TIMER HIT`, `[PMIRQSB] IRQ HIT`, and `[PMIRQSB] PASS`, proving protected-mode SB IRQ7 delivery works in the standalone DOS/4GW probe.
- `DO_BUILD=0 LOG_FILE=build/full/qemu-pmirqsb-variants-irq7b.log QEMU_STDERR=build/full/qemu-pmirqsb-variants-irq7b.stderr QEMU_CMD_LOG=build/full/qemu-pmirqsb-variants-irq7b.commands.log DOS_TAXONOMY_USE_CASE=generic DOS_TAXONOMY_PROFILE=dos_generic DOS_TAXONOMY_MIN_STAGE=transfer_marker DOS_TAXONOMY_APP_DIR_IN_IMAGE=::SYSTEM/DRIVERS DOS_TAXONOMY_APP_BINARY_NAME=PMIRQSB.COM DOS_TAXONOMY_CWD='\SYSTEM\DRIVERS' DOS_TAXONOMY_RUN_COMMAND='run PMIRQSB.COM' DOS_TAXONOMY_APP_RUNTIME_MARKERS='\[PMIRQSB\][[:space:]]+PASS' DOS_TAXONOMY_RUN_DRVLOAD=0 QEMU_AUDIO_MODE=on QEMU_AUDIO_BACKEND=alsa QEMU_TIMEOUT_SEC=260 bash scripts/qemu_test_full_dos_taxonomy.sh` PASS at `transfer_marker`; IRQ7 single ACK-before-EOI, single EOI-before-ACK, and auto-init variants all show `DMA_BOUNDARY OK`, `TIMER HIT`, `IRQ HIT`, and final `PASS`.
- `DO_BUILD=0 LOG_FILE=build/full/qemu-pmirqsb-variants-irq5.log QEMU_STDERR=build/full/qemu-pmirqsb-variants-irq5.stderr QEMU_CMD_LOG=build/full/qemu-pmirqsb-variants-irq5.commands.log DOS_TAXONOMY_USE_CASE=generic DOS_TAXONOMY_PROFILE=dos_generic DOS_TAXONOMY_MIN_STAGE=transfer_marker DOS_TAXONOMY_APP_DIR_IN_IMAGE=::SYSTEM/DRIVERS DOS_TAXONOMY_APP_BINARY_NAME=DOS4GW.EXE DOS_TAXONOMY_CWD='\SYSTEM\DRIVERS' DOS_TAXONOMY_RUN_COMMAND='run DOS4GW.EXE PMIRQSB.LE IRQ5' DOS_TAXONOMY_APP_RUNTIME_MARKERS='\[PMIRQSB\][[:space:]]+PASS' DOS_TAXONOMY_RUN_DRVLOAD=0 QEMU_AUDIO_MODE=on QEMU_AUDIO_BACKEND=alsa QEMU_SB_IRQ=5 QEMU_TIMEOUT_SEC=260 bash scripts/qemu_test_full_dos_taxonomy.sh` PASS at `transfer_marker`; IRQ5 single ACK-before-EOI, single EOI-before-ACK, and auto-init variants all show `DMA_BOUNDARY OK`, `TIMER HIT`, `IRQ HIT`, and final `PASS`.
- `DO_BUILD=0 LOG_FILE=build/full/qemu-pmirqsb-prime.log QEMU_STDERR=build/full/qemu-pmirqsb-prime.stderr QEMU_CMD_LOG=build/full/qemu-pmirqsb-prime.commands.log DOS_TAXONOMY_USE_CASE=generic DOS_TAXONOMY_PROFILE=dos_generic DOS_TAXONOMY_MIN_STAGE=transfer_marker DOS_TAXONOMY_APP_DIR_IN_IMAGE=::SYSTEM/DRIVERS DOS_TAXONOMY_APP_BINARY_NAME=PMIRQSB.COM DOS_TAXONOMY_CWD='\SYSTEM\DRIVERS' DOS_TAXONOMY_RUN_COMMAND='run PMIRQSB.COM' DOS_TAXONOMY_APP_RUNTIME_MARKERS='\[PMIRQSB\][[:space:]]+PASS' DOS_TAXONOMY_RUN_DRVLOAD=0 QEMU_AUDIO_MODE=on QEMU_AUDIO_BACKEND=alsa QEMU_TIMEOUT_SEC=260 bash scripts/qemu_test_full_dos_taxonomy.sh` PASS at `transfer_marker`; the lightweight priming path runs only the IRQ7 single ACK-before-EOI transfer and reaches `IRQ HIT`/`PASS`.
- `CIUKIOS_DOOM_AUDIO_PROFILE=sb16-sfx make build-full` PASS, followed by `PMIRQSB` lightweight priming before `run DOOM.EXE`: `PMIRQSB` reaches `PASS`, DOOM still reaches `I_StartupSound`, `calling DMX_Init`, `S_Init`, and `HU_Init`, then remains `runtime_stable=PASS` but `visual_gameplay=FAIL` with the same low-diversity startup screenshot; this rules out simple prelaunch IRQ/DMA priming as the missing DOOM SFX fix.
- A conservative `snd_channels=1` SB16 SFX config and a separate validated IRQ5 lane (`QEMU_SB_IRQ=5`, `snd_sbirq=5`) both still leave DOOM at `runtime_stable=PASS` and `visual_gameplay=FAIL`; the remaining blocker is therefore inside DOOM/DMX's active SB SFX runtime path rather than raw IRQ/DMA routing, AdLib/music fallback, channel count, or Stage1 INT31 emulation.
- `make qemu-test-full` PASS and `make qemu-test-full-cd` PASS after the PMIRQSB protected-mode wait fix, with ALSA audio detail and Live CD prompt `D:`.
- `third_party/Doom/DOOM.EXE` identified as embedded DOS/4G executable SHA256 `799a20c759567cebb530b7d8b1e7765b13734be5af7f97367f6aa81d87b636da`, MD5 `c5ade1b30eb1d932921d0eebac8c6d07`, size `694K`; the real LE payload starts at file offset `0x25214`, and the new `handoff/2026-05-15-doom-dmx-sb16-reverse-handoff.md` records the current SB/DMX evidence and next minimal probe target.
- `make build-full` PASS after adding `PMIRQSB TASK`; `verify-full-drivers-payload` remains PASS with 71 driver files.
- `make build-full-cd` PASS after carrying the updated `PMIRQSB TASK` payload into the full-CD image set.
- `DO_BUILD=0 LOG_FILE=build/full/qemu-pmirqsb-task.log QEMU_STDERR=build/full/qemu-pmirqsb-task.stderr QEMU_CMD_LOG=build/full/qemu-pmirqsb-task.commands.log DOS_TAXONOMY_USE_CASE=generic DOS_TAXONOMY_PROFILE=dos_generic DOS_TAXONOMY_MIN_STAGE=transfer_marker DOS_TAXONOMY_APP_DIR_IN_IMAGE=::SYSTEM/DRIVERS DOS_TAXONOMY_APP_BINARY_NAME=DOS4GW.EXE DOS_TAXONOMY_CWD='\SYSTEM\DRIVERS' DOS_TAXONOMY_RUN_COMMAND='run DOS4GW.EXE PMIRQSB.LE TASK' DOS_TAXONOMY_APP_RUNTIME_MARKERS='\[PMIRQSB\][[:space:]]+TASK PASS' DOS_TAXONOMY_RUN_DRVLOAD=0 QEMU_AUDIO_MODE=on QEMU_AUDIO_BACKEND=alsa QEMU_TIMEOUT_SEC=260 bash scripts/qemu_test_full_dos_taxonomy.sh` PASS at `transfer_marker`; fresh markers show `TASK INSTALL`, three `TASK HIT`/`FXDMA START` dispatches, three `FXDMA IRQ` acknowledgements, `TIMER HIT`, `TASK PASS`, and final `PASS`. `runtime_stable` is intentionally FAIL because the probe exits back to the shell after completion.

## pre-Alpha v0.6.6 (2026-05-08)

1. Rebased the roadmap after Phase 4: DOOM visual gameplay is closed, while Stage1/runtime split work, broader DOS app compatibility, legacy audio, and full/full-CD hardening are the next priorities.
2. Advanced the Stage1/runtime split foundation with `\SYSTEM\RUNTIME.BIN`, runtime service-table probing, callable service ids 1-5, corrupt-runtime fallback checks, default-drive state bridging, and Stage1 size recovery.
3. Hardened full-profile DOS compatibility across C:/D: drive state, per-drive CWD, FAT16 path/create/open/delete/rename behavior, INT 21h country/IOCTL/switch/PSP/handle/memory services, and external app return-state handling.
4. Expanded external DOS application evidence with CIUKEDIT/GFXSTAR smoke coverage, optional DOSNavigator packaging/startup validation, shell chrome isolation, temporary INT 10h/INT 33h external-app handling, and DOSNavigator-focused stability fixes.
5. Restored and revalidated DOOM startup/gameplay after memory-map and allocator regressions; improved DOOM/DOS taxonomy lanes with honest `runtime_stable` and `visual_gameplay` classification.
6. Hardened the full-CD Live/install path with the direct El Torito ISO as primary output, visual SETUP UI, destructive HDD install flow, `FORMAT.COM`, topology guards, eject-before-reboot prompt, batched install I/O, and ThinkPad T23 real-hardware follow-ups.
7. Added public docs for DOS compatibility and legacy audio planning, updated support links, cleaned obsolete project artifacts, and aligned release-facing metadata for `CiukiOS pre-Alpha v0.6.6`.

## pre-Alpha v0.6.5 (2026-05-05)

1. Established the Stage1/runtime split as the structural direction: Stage1 remains loader-first while runtime, shell, driver/CD policy, diagnostics, and module responsibilities migrate toward loaded components under `\SYSTEM`.
2. Added the inert `src/runtime/runtime.asm` artifact, packaged it as `\SYSTEM\RUNTIME.BIN`, documented the split plan, and kept default full/full-CD boot behavior stable.
3. Validated the slice across active full/full-CD build, QEMU smoke, shell, driver, setup, Stage1 selftest, runtime-probe, and DOOM taxonomy lanes.
4. Updated version, banner, ISO label, README, roadmap, and release metadata for `CiukiOS pre-Alpha v0.6.5`.

## pre-Alpha v0.6.3 (2026-05-05)

1. Promoted the full-CD profile into the main Live/install media path with direct El Torito hard-disk boot, D: live shell behavior, SETUP destructive install support, and disposable HDD install validation.
2. Added CHS/EDD boot and setup hardening, CD/HDD probe lanes, installed-HDD boot checks, and user-facing QEMU run/test targets for the full-CD profile.
3. Improved shell and FAT16 DOS behavior around drive semantics, current-directory preservation, prompt recovery, case handling, footer telemetry, and repeated COM/EXE execution stability.
4. Added DOOM `runtime_stable` taxonomy classification so post-video observation failures are reported honestly instead of being hidden behind earlier startup stages.

## pre-Alpha v0.6.1 (2026-05-04)

1. Closed Phase 4 as DOOM gameplay playable on the full FAT16 profile: DOOM launches through DOS/4GW, loads WAD data, reaches video/gameplay runtime, and was manually confirmed playable by the project owner.
2. Added the full-profile DOOM taxonomy harness, local-only DOOM payload packaging, and staged runtime fixes across MZ loading, FAT16 read/seek behavior, PSP/MCB setup, DOS memory strategy, XMS move support, and DOS extender startup classification.
3. Closed the Phase 4 installer execution lane with deterministic setup scenario coverage and release-facing documentation updates.

## pre-Alpha v0.5.4 (2026-05-01)

1. Improved shell input stability for hold-key repeat, line wrap, and backspace behavior.
2. Stabilized FAT16 shell footer telemetry and revalidated cross-profile build/regression lanes.

## pre-Alpha v0.5.3 (2026-04-30)

1. Stabilized shell `move`/`mv` behavior across floppy and full runtime profiles.
2. Fixed `INT 21h AH=56h` rename/move behavior and revalidated shell command regression lanes.

## pre-Alpha v0.5.2 (2026-04-29)

1. Closed Stage1 DOS command regressions across floppy and full profiles.
2. Stabilized critical INT 21h read/write/seek return paths and floppy image write behavior.

## pre-Alpha v0.5.0 (2026-04-28)

1. Stabilized Stage1 startup, shell entry, prompt/input paths, drive/CWD state, and QEMU stderr observability for more deterministic bring-up.

## pre-Alpha v0.5.0 (2026-04-22)

1. Restarted the project from a clean legacy BIOS x86 architecture baseline.
2. Introduced the initial `floppy` and `full` build profiles with build/QEMU smoke scripts and baseline branch/documentation discipline.
